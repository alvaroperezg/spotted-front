export interface DownloadItem {
    id: string
    title: string
    photographer: string
    photoDate: string
    downloadedDate: string
    resolution: string
    format: string
    size: string
    orderNumber: string
    imageUrl: string
  }
